#include <stdio.h>
#include <dirent.h>

int main(int argc, char *argv[])
{
	DIR *dp;
	struct dirent *dirp;

	if(argc != 2)
	{
		fprintf(stdout, "using: ls <dir_name>\n");
		return 1;
	}

	if( (dp = opendir(argv[1])) == NULL )
	{
		perror("opendir");
		return 1;
	}
}
