#include <stdio.h>

int main(int argc, char *argv[])
{
	int c;

	while ( (c = getc(stdin) ) != EOF )
	{
		if( putc(STDOUT_FILENO, buf, n) == n)
		{
			perror("write");
			return 1;
		}
	}
	if(n < 0)
	{
		perror("read");
		return 1;
	}

	return 0;
}
