#include <stdio.h>
#include <unistd.h>

const unsigned int BUFFSIZE = 4096;

int main(int argc, char *argv[])
{
	int n;
	char buf[BUFFSIZE];

	while ( (n = read(STDIN_FILENO, buf, BUFFSIZE) ) > 0 )
	{

	}

	return 0;
}
