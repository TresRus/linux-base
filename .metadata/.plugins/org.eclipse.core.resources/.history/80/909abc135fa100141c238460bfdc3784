#include <stdio.h>

int main(int argc, char *argv[])
{
	int c;

	while ( (c = getc(stdin) ) > 0 )
	{
		if( write(STDOUT_FILENO, buf, n) != n)
		{
			perror("write");
			return 1;
		}
	}
	if(n < 0)
	{
		perror("read");
		return 1;
	}

	return 0;
}
